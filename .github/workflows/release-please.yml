name: Release Please

on:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: googleapis/release-please-action@45996ed1f6d02564a971a2fa1b5860e934307cf7 # v5.0.0
        id: release
        with:
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json

      # Publish is inlined here (not a separate workflow on release:created)
      # because GitHub does not trigger downstream workflows from events
      # authored by the default GITHUB_TOKEN — release-please-action's
      # GitHub release never fires release:created, so the previous
      # publish.yml never ran.
      #
      # `releases_created` (plural) is what the action sets in manifest
      # mode; the singular `release_created` only exists in non-manifest
      # mode and would silently skip every step here.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        if: ${{ steps.release.outputs.releases_created == 'true' }}

      - uses: ./.github/actions/setup
        if: ${{ steps.release.outputs.releases_created == 'true' }}

      - run: pnpm run lint
        if: ${{ steps.release.outputs.releases_created == 'true' }}

      - run: pnpm run typecheck
        if: ${{ steps.release.outputs.releases_created == 'true' }}

      - run: pnpm test
        if: ${{ steps.release.outputs.releases_created == 'true' }}

      - run: pnpm run build:clean
        if: ${{ steps.release.outputs.releases_created == 'true' }}

      # Trusted Publisher (OIDC) needs npm CLI >= 11.5.1; Node 22 ships
      # with npm 10.x. Run the modern CLI via `npx` from its separate
      # cache instead of `npm install -g npm@latest`, which races against
      # the running npm mid-install ("Cannot find module 'promise-retry'").
      # npm 11+ also requires an explicit --tag for prereleases.
      #
      # The CLI version is pinned exactly, never `@latest`: this step runs
      # with the OIDC publish credential live, so whatever npx resolves
      # here executes with publish rights. A published npm version is
      # immutable; a dist-tag is not. Dependabot does not track this
      # line, so bump it by hand. Stay on 11.x: npm 12 raises the Node
      # floor to ^22.22.2, which `node-version: 22` does not guarantee.
      - name: Publish to npm
        if: ${{ steps.release.outputs.releases_created == 'true' }}
        env:
          NPM_CLI_VERSION: 11.19.1
        run: |
          version=$(node -p "require('./package.json').version")
          if [[ "$version" == *-* ]]; then
            tag="${version#*-}"
            tag="${tag%%.*}"
            npx -y "npm@${NPM_CLI_VERSION}" publish --provenance --access public --tag "$tag"
          else
            npx -y "npm@${NPM_CLI_VERSION}" publish --provenance --access public
          fi
